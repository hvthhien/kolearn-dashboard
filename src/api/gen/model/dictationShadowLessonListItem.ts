/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

/**
 * One shadowing lesson, as a row on the "từ bài nhại theo" shelf. The
 * same fields as `DictationSetListItem` under the same names, so one row
 * component draws both shelves; `thumbnailUrl` is the one addition.
 */
export interface DictationShadowLessonListItem {
  id: string;
  title: string;
  /**
     * The line windows added up, not the track's length — what the
     * learner will actually hear, with the silence between lines left
     * out.
     */
  durationMs: number;
  /** Lines. Called `itemCount` because to the learner both shelves count "câu". */
  itemCount: number;
  /** Entries in the lesson's own dictionary. */
  wordCount: number;
  /**
     * TCCN-427-1's numerator over the learner's DICTATION results on this
     * lesson — not `practisedCount`, which is what the shadowing shelf
     * shows for the same rows. A gần đúng counts; a skipped line has no
     * row.
     */
  correctCount: number;
  /**
     * Whether the caller may open this lesson, decided by the SHADOWING
     * catalogue: a lesson an operator has opened is free to type as well
     * as to repeat, and every other one is refused with
     * `403 premium_required` on both screens. One flag, one lesson — the
     * two screens cannot disagree about it.
     */
  locked: boolean;
  /** The lesson's poster, as on `ShadowVideoListItem`. */
  thumbnailUrl?: string;
  /**
     * The band, as on `ShadowVideoListItem` and read off the same
     * SHADOWING catalogue row — so a lesson badged `Cấp 3` on the nhại
     * theo shelf is badged `Cấp 3` here. Optional, with the same
     * no-badge fallback.
     * @minimum 1
     * @maximum 6
     */
  level?: number;
  /** A `ShadowCategory` id. Absent means uncategorised. */
  categoryId?: string;
  categoryName?: string;
  /** Nhãn. Never null; `[]` on a lesson nobody has tagged (R-13). */
  tags: string[];
}
