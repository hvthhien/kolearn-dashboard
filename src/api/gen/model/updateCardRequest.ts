/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

/**
 * A partial edit. **No field is required**, and an absent field means
 * "leave it" rather than "clear it" — which is why `back: ""` has to be
 * expressible and means exactly what it says: the learner is removing a
 * meaning they had written. Sending neither field is 422 rather than a
 * silent no-op.
 *
 * Only these two. Not `state` — that is `PUT /cards/{cardId}/state`, and
 * keeping them apart is what stops an edit from quietly re-marking a card
 * the learner has been working through. Not `topics`, which a card
 * inherits once from the question that made it. And none of R-15's three
 * unused scheduling columns, which nothing in this product writes.
 */
export interface UpdateCardRequest {
  /**
     * Editable only on a card the learner typed themselves. A card that
     * came from a question, a video line, a dictation sentence or a
     * paper's bảng từ vựng answers `card_front_locked` (409) instead.
     *
     * The reason is that `front` is the card's identity twice over: the
     * deck dedupes on `normalize_text(front)`, and the wordbook screen
     * finds a learner's card by that same key — so changing it on a card
     * saved from a paper would flip that entry back to "Chưa lưu" while
     * the card still names the paper, with nothing failing. Worse, the
     * card's provenance would go on citing a question that no longer
     * contains the word, and an unfollowable citation is the failure
     * nobody reports as a bug because it looks deliberate.
     *
     * A whitespace-only change keeps the same normalized key and is
     * allowed on any card.
     * @minLength 1
     */
  front?: string;
  /**
     * The meaning the learner saved. Free to edit on any card, including
     * one from a corpus — the dictionary material lives in
     * `wordbookEntry` and is never written from here.
     */
  back?: string;
}
