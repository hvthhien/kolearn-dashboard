/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

export interface SignedInSession {
  /**
     * The refresh-token family. What `DELETE /me/sessions/{sessionId}`
     * takes, and what survives every rotation of the token behind it.
     */
  id: string;
  /**
     * Whether this is the device making the request. Exactly one row
     * carries it, or none — see the note on `GET /me/sessions` for the
     * fifteen minutes after a deploy when it is none.
     */
  current: boolean;
  /**
     * Exactly what the browser sent, unparsed and possibly empty.
     *
     * Deliberately not turned into "Chrome trên macOS" here. That string
     * is presentation — it would put Vietnamese in the API — and it is
     * guesswork besides: user-agent parsing is a pile of heuristics that
     * are wrong often enough that the raw value has to survive as the
     * fallback. The client reduces it and decides what to show when it
     * cannot.
     */
  userAgent: string;
  /** When this device first signed in. */
  startedAt: string;
  /**
     * The most recent token this family was issued, which is the last time
     * the device renewed. Within about fifteen minutes of "last used",
     * and not a claim of anything finer.
     */
  lastSeenAt: string;
}
