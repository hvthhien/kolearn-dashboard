/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */
import type { AssistantVocabItemKind } from './assistantVocabItemKind';

/**
 * One term, in the shape `POST /cards` takes: `word` is the front,
 * `meaning`, `transcription` and `example` are the back, and `kind` is the
 * card type.
 *
 * No id of any sort. A thẻ made from an answer has no source row to point
 * at — unlike one saved from a bảng từ vựng, a shadowing line or a
 * dictation item — so `POST /cards:batch` is called with none of its
 * `source*` fields.
 */
export interface AssistantVocabItem {
  /**
     * The card type this becomes. `STRUCTURE_TEMPLATE` is deliberately not
     * here: that is R-13's writing scaffold, made from a real essay on the
     * writing screens, and a chat answer has no business minting one.
     */
  kind: AssistantVocabItemKind;
  /**
     * The Korean, as it goes on the front of the card — a headword in
     * dictionary form, or a pattern as a textbook writes it
     * (`-(으)려고 하다`). Always contains Hangul; the server drops any item
     * whose word does not, because the front of a thẻ is what a phiên ôn
     * shows and a Vietnamese one is a card the learner has already
     * answered.
     */
  word: string;
  /** The Vietnamese, and the first line of the back. Never empty. */
  meaning: string;
  /**
     * Phiên âm — how `word` is SAID, in Revised Romanisation with syllables
     * joined by hyphens (접수 → `jeop-su`, 좋아요 → `jo-a-yo`). It becomes
     * the card's own `transcription`.
     *
     * Absent when the model is not sure, which the prompt asks for by name
     * and is a real answer: the card then shows no reading, and that costs
     * the learner nothing. A guessed one costs them a great deal — it sits
     * on the back of a card they may be shown every morning for weeks,
     * they have no way to tell it is wrong, and what they practise is the
     * wrong pronunciation. `PATCH /cards/{cardId}` is how a reading gets
     * fixed once a learner spots it.
     *
     * Asked of the model rather than derived here because romanisation is
     * not a function of the spelling: 좋아요 is `joh-a-yo` letter by letter
     * and `jo-a-yo` out loud, 신라 is `sil-la`. The cards that need a
     * reading most are exactly the ones no corpus in this app describes.
     */
  transcription?: string;
  /**
     * One Korean sentence using it, or absent. Optional because it
     * genuinely is — a pattern explained in prose often has no single
     * sentence worth lifting out, and an invented one would be worse than
     * none.
     */
  example?: string;
}
