/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

/**
 * Every property optional: absent means "leave this one alone". See
 * `PATCH /me/profile` for why this is a patch rather than a put.
 */
export interface UpdateProfile {
  /**
     * What the learner is called on screen.
     *
     * **Cleaned before it is stored, not refused.** The value is
     * normalised to NFC, characters that would not print are dropped, and
     * every run of whitespace becomes one space with the ends trimmed. A
     * name arriving with a trailing space, a tab pasted out of a
     * spreadsheet, or a newline from a box that wrapped is a paste
     * accident rather than a request for those characters, and answering
     * 422 would send the learner hunting for something invisible.
     *
     * **NFC first, so the limit counts what a reader would call a
     * character.** A browser may send `ế` as one rune or as two, and
     * Vietnamese names are full of them; measuring before composing would
     * give one name two different lengths depending on the keyboard that
     * typed it. `maxLength` here is characters after the cleaning.
     *
     * Two results have no reasonable repair and are refused as 422:
     * nothing at all left once cleaned (`display_name_empty` — a learner
     * may not become nameless), and longer than the limit
     * (`display_name_too_long` — guessing where to cut somebody's name is
     * worse than saying it does not fit).
     *
     * Accounts registered before this endpoint existed may hold a name
     * that would fail both rules, because `POST /auth/register` has never
     * checked one. `GET /me` still returns whatever is stored; it is
     * brought into line the first time its owner writes here.
     * @maxLength 60
     */
  displayName?: string;
}
