/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

/**
 * How many lessons sit under each status chip.
 *
 * **Narrowed by every filter except `status` itself** — the guarantee
 * `ExamLevelCounts` carries, for the reason written there: a chip must
 * say how many rows are under it whichever chip is currently pressed, and
 * counts taken from an already-filtered response read zero everywhere but
 * the active one. So `q` and `categoryId` and `level` all shrink these
 * three numbers, because a learner who has typed a word is asking about
 * their search rather than about the corpus; `status` does not, because
 * it is the axis these numbers ARE.
 *
 * Keyed by the `LessonStatus` value, so a client indexes it with the
 * chip's own value rather than keeping a second table mapping one to the
 * other.
 *
 * No "Tất cả" key. Every row has exactly one of these three, so the sum
 * is that number and a fourth field would be one more thing that can
 * disagree with the other three.
 */
export interface LessonStatusCounts {
  NOT_STARTED: number;
  IN_PROGRESS: number;
  COMPLETED: number;
}
