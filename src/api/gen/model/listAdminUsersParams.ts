/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */
import type { AdminUserStatus } from './adminUserStatus';

export type ListAdminUsersParams = {
/**
 * Matches the address by prefix, or the display name anywhere in it,
 * case-insensitively. Absent lists everybody.
 *
 * Prefix on the address rather than substring on purpose: an operator
 * types what somebody told them on the phone, from the front, and a
 * substring match over every address is a scan of the whole table for
 * a result nobody asked for.
 * @maxLength 200
 */
q?: string;
/**
 * Only accounts holding this role. Absent means any.
 */
role?: string;
/**
 * Only accounts in this state. Absent means any, **including
 * DELETED** — an account somebody is asking about is one this list
 * has to be able to show.
 */
status?: AdminUserStatus;
/**
 * @minimum 1
 * @maximum 100
 */
limit?: number;
/**
 * Rows to skip, over the same newest-first order. The order breaks
 * ties on `id`, which is unique, so an account cannot land on two
 * pages at once or fall between them.
 * @minimum 0
 */
offset?: number;
};
