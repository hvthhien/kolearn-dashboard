/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

/**
 * What an endpoint that has just posted a six-digit code answers with.
 *
 * It carries no secret — the code is in the learner's inbox and nowhere
 * else — only the two numbers the screen needs to draw itself. And it is
 * built from constants, so it is IDENTICAL for a send that happened and
 * one that was suppressed. That is deliberate: a body that varied would
 * be an account-enumeration oracle on `/auth/password/forgot`.
 */
export interface VerificationChallenge {
  /**
     * The normalised address the code went to — casefolded and trimmed,
     * which is not always what was typed. The client shows this on the
     * code screen so a learner who mistyped can see it and go back.
     */
  email: string;
  /** Seconds until the code stops working. */
  expiresIn: number;
  /**
     * Seconds before a resend will actually send anything. The client
     * disables its resend button for this long; the server enforces the
     * same wait regardless of what the client does, so a caller that
     * ignores this simply gets 202s that send nothing.
     */
  resendAfter: number;
}
