/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */

/**
 * How many published papers each level holds, for the numbers on
 * SC-TEST-LIST's chips.
 *
 * Keyed by the `ExamLevel` value so a client can index it with the chip's
 * own value rather than keeping a second table mapping one to the other.
 *
 * **Never narrowed by the `level` parameter.** A chip has to say how many
 * papers sit under it whichever chip is currently pressed — counts taken
 * from an already-filtered response would read zero everywhere but the
 * active one, which is the same argument `GET /shadowing/categories`
 * makes for being a request of its own.
 *
 * Nothing here counts "Tất cả": every published paper has exactly one of
 * these two levels, so the sum is that number and a third field would be
 * one more thing that can disagree with the other two.
 */
export interface ExamLevelCounts {
  TOPIK_I: number;
  TOPIK_II: number;
}
