/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */
import type { LessonStatus } from './lessonStatus';

export type ListShadowDictationLessonsParams = {
/**
 * Clamped rather than refused, as everywhere else here.
 * @minimum 1
 * @maximum 200
 */
limit?: number;
/**
 * Lessons to skip, over the same total order the page returns. Clamped like `limit`.
 * @minimum 0
 */
offset?: number;
/**
 * A `ShadowCategory` id, from a tapped chip. Absent is "Tất cả".
 */
categoryId?: string;
/**
 * Tìm kiếm over the lesson's title, its chủ đề name and its nhãn.
 * **Not the transcript** — and this is where this list deliberately
 * stops short of `/shadowing/videos`, whose `q` does reach the lines.
 * On that screen the line is on screen while the learner repeats it;
 * on this one it has to be typed from the audio first (TCCN-421-1),
 * and a box that matched it would answer a guessed spelling before
 * the learner had typed a character into the exercise.
 *
 * Trimmed, and blank is absent.
 * @maxLength 80
 */
q?: string;
/**
 * TOPIK band, from a tapped chip — the learner narrowing their own shelf.
 * @minimum 1
 * @maximum 6
 */
level?: number;
/**
 * The learner's own DICTATION progress on each lesson, read from
 * `correctCount` against `itemCount` the way the row's pill is.
 */
status?: LessonStatus;
};
