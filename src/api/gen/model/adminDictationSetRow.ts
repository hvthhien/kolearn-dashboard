/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kolearn API
 * API for Kolearn, a TOPIK exam-prep platform.
 *
 * Covers Phase 1's thin slice: browse exams, take one, submit, read the
 * result, review each answer with its five explanation layers, tag topics,
 * and turn wrong answers into review cards.
 *
 * Two conventions run through the whole surface:
 *
 * **Errors are RFC 9457 problem+json.** Every non-2xx response — including
 * unmatched routes — has `type`, `title`, `status`, and `code`. Some carry
 * extension members: a 409 from `POST /attempts` includes the in-progress
 * attempt so the client can render its confirmation dialog without a second
 * request.
 *
 * **A machine-derived score never appears as a bare number.** Bands and
 * writing scores are `EstimatedBand` / estimate-tagged objects that carry
 * their own disclaimer, so a client cannot render the value without having
 * been handed the caveat.
 *
 * OpenAPI spec version: 0.1.0
 */
import type { AdminDictationReviewSummary } from './adminDictationReviewSummary';
import type { AdminDictationSetRowStatus } from './adminDictationSetRowStatus';
import type { AdminDictationSetRowVoiceKind } from './adminDictationSetRowVoiceKind';

export interface AdminDictationSetRow {
  id: string;
  title: string;
  /**
     * @minimum 1
     * @maximum 6
     */
  level: number;
  voice: string;
  voiceKind: AdminDictationSetRowVoiceKind;
  status: AdminDictationSetRowStatus;
  publishedAt?: string;
  review: AdminDictationReviewSummary;
  /**
     * Whether gói Cơ bản is shut out of this set. Not the learner's
     * `locked`, which is the same fact seen from the caller's side: a
     * premium learner reading a premium set gets `locked: false`.
     *
     * True on everything migration 00056 found, and true on anything
     * created since — an item is part of the paid catalogue until an
     * operator says otherwise, because the other default would open the
     * corpus by accident every time a row was added.
     *
     * Changed by `PUT /admin/dictation/sets/{setId}/premium`, under `billing:manage`.
     */
  premium: boolean;
  /** Chủ đề. Absent means uncategorised — a warning at the publish gate, never a blocker. */
  categoryId?: string;
  categoryName?: string;
  /** Never null. The same labels the learner sees, in the same order. */
  tags: string[];
}
